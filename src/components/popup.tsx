import { PopUpProps } from "@/types/types";

/*
    Create a popup which disappears after a given amount of time.
    Default duration: 200
    Props' children is a string which is displayed as the popup's text
*/

export default function Popup(popUpProps: PopUpProps) {}
